C99
implementar codificacoes e decodificacoes de:
		Base 62 (Maze62), 
		Base 64 (Wikipedia) , 
		Base 85 (Wikipedia) , 
		Base 91 (Dake He) para binario.
		Relatorio

Pode fazer em separado

Os parâmetros (nomes dos arquivos e opção de codificação ou decodificação) devem ser passados por linha de comando
durante o lançamento do programa ou, caso algum dos parâmetros não tenham sido fornecidos, solicitados ao usuário 
durante a execução. 

cada um dos algoritmos valha 1/6 da nota, totalizando 4/6, 
a descrição de cada algoritmo e a aplicação dele ao mesmo texto exemplo valerá 1/6,
por fim, um relatório com a comparação e a análise das diferenças e onde usar cada um deles e porque, valerá o 1/6 restante da nota.

Comparar numa mesma máquina os tempos de execução de cada uma das codificações, tanto para codificação quanto para decodificação.

comparar o tamanho dos arquivos codificados.

passagem de parâmetros (0,5 ponto para entrada via linha de comando e, 0,5 ponto para entrada via usuário), 
codificação (3 pontos), decodificação (3 pontos), descrição da lógica e estruturação (1 ponto), documentação (1 ponto), 
qualidade e escalabilidade da implementação (1 ponto) 

organização (1 ponto), comparação dos resultados, incluindo gráficos, rodar múltiplas vezes, 
estimar erro (5 pontos),  analisar e especificar possíveis melhorias das implementações (2 pontos), 
listar vantagens e desvantagens de cada uma das codificações (2 pontos).

corretude do algoritmo (1,5 pontos para cada algoritmo), corretude da cifra do texto exemplo (1 ponto para cada algoritmo).

zipados em um arquivo denominado 1-C-grupo<número do grupo>.zip. Para testar os programas feitos, utilize os arquivos 
zipados disponíveis no arquivo denominado arquivos_teste.zip

Barbara -> 1
Gabriel -> 2
Jadiel -> 3
Marcos -> 4
Tulio -> 5

Pessoal, seria bom já dividirmos o de trabalho em C
A princípio temos que fazer o seguinte:

1 - Base 62, codificar, decodificar, descrição, aplicação ao texto, documentação, tempos de exec, tam dos arq, rodar várias vezes.
2 - Base 64, codificar, decodificar, descrição, aplicação ao texto, documentação, tempos de exec, tam dos arq, rodar várias vezes.
3 - Base 85, codificar, decodificar, descrição, aplicação ao texto, documentação, tempos de exec, tam dos arq, rodar várias vezes.
4 - Base 91, codificar, decodificar, descrição, aplicação ao texto, documentação, tempos de exec, tam dos arq, rodar várias vezes.
5 - Relatorio, comparação, análise das diferenças e onde usar, gráficos, vantagens e desvantagens.

Se faltar qlq coisa avisem, se quiserem dividir de outra forma também.

Barbara -> 5
Gabriel -> 4
Jadiel -> 3
Marcos -> 2
Tulio -> 1